const NAME = 'tabulate';
const root = `./plugins/${NAME}`;

export const PATH = {
  root,
  apps: `${root}/apps`,
  config: `${root}/config`,
  defSet: `${root}/defSet`,
  data: `${root}/resources/data`,
};
